# AI 协作入口

本文是 AI 代理进入 RebootSnap 仓库后的操作入口。人类维护者也可以用本文检查 AI 是否按项目规则工作。

## 必读顺序

开始任何修改前，按顺序读取：

1. `docs/index.md`：了解文档地图和规范入口。
2. `docs/project-governance.md`：确认人与 AI 的职责边界。
3. `docs/change-management.md`：确认提交信息、变更记录和校验要求。
4. `docs/glossary.md`：确认稳定术语。
5. `docs/project-map.yml`：获取机器可读的项目地图。

首次进入新 clone 的仓库时，先运行 `scripts/setup-dev.sh` 启用本地 Git hook。

## AI 执行闭环

AI 处理一次任务时，按以下闭环执行，除非用户明确只要求分析或讨论：

1. 先按必读顺序加载上下文，确认当前任务触及哪些稳定入口、设计约束和人类确认边界。
2. 如果任务触及“人类确认边界”，先停止并说明需要确认的具体事项；不要用实现细节绕过确认。
3. 修改时保持入口一致：文档入口、机器地图、术语表、变更记录和校验脚本需要同步时必须一起更新。
4. 对设计类内容，先保持分类、边界和术语稳定，再考虑采集项、字段、命令或实现。
5. 完成后运行 `scripts/verify.sh`，并在输出中区分自动校验结果和人工审核结论。
6. 如果提交或推送，提交信息必须与 `CHANGELOG.md` 顶部条目一致。

## 工作规则

- 优先遵守项目文档中的明确规则，不用临时偏好覆盖已固定规范。
- 修改任何面向项目历史有意义的内容时，必须更新 `CHANGELOG.md` 顶部条目。
- 新增或移动文档时，必须更新 `README.md`、`docs/index.md` 和 `docs/project-map.yml` 中对应入口。
- 引入新稳定术语前，先检查 `docs/glossary.md`；确实需要新增时，同步更新术语表。
- 建立或改变设计约束时，必须在变更记录的 `设计影响` 中写清后续约束。
- 如果本地 hook 未启用，先运行 `scripts/setup-dev.sh`。
- 提交前必须运行 `scripts/verify.sh`；启用本地 hook 后，提交时也会自动运行该校验。
- 自动校验只能证明结构、链接、入口和提交规则满足脚本约束；设计语义、事实准确性和边界取舍仍需 AI 明确说明审核依据。

## 人类确认边界

AI 可以自主处理局部修正文档、补齐索引、添加校验和修复明显格式错误。

以下事项必须先获得人类确认：

- 改变项目目标、适用范围或核心定义。
- 删除或重命名稳定 ID、稳定术语、规范入口或历史变更记录。
- 引入新的外部依赖、发布流程、数据持久化策略或安全边界。
- 大规模重写已有设计文档，导致原有约束难以追溯。
- 绕过 `scripts/verify.sh`、Git hook 或 CI 的失败结果。

## 输出要求

- 说明修改了哪些稳定入口和为什么。
- 明确验证命令及结果。
- 区分脚本自动验证、人工内容审核和无法本地确认的远端治理状态。
- 对未完成、未验证或需要人类决策的事项直接标出。
